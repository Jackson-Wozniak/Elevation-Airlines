export interface FlightTableEntry{
    callsign: string,
    departureCode: string,
    destinationCode: string,
    departureTime: string,
    arrivalTime: string,
    planeType: string
    status: string
}